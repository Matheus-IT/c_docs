#include <stdio.h>
#include <stdlib.h>
#include <errno.h>

int main() {
    /*
            *** The #define Directive ***
        - The #define directive is used to create object-like macros for constants based on
        values or expressions.
        - #define can also be used to create function-like macros with arguments that will
        be replaced by the preprocessor.
        #define PI 3.14
        #define AREA(r) (PI*(r)*(r))
    */
    return 0;
}
