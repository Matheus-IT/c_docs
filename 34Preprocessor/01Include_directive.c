#include <stdio.h>
#include <stdlib.h>
#include <errno.h>

int main() {
    /*
            *** The #include Directive ***
        - The #include directive is for including header files in a program. A header file
        declares a collection of functions and macros for a library, a term that comes from
        the way the collection of code can be reused. 
        #include <something.h>
        #include "myutils.h"
        #include "somefile.c"
    */
    return 0;
}
